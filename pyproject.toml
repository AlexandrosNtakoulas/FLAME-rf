[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flame-rf"
version = "0.1.0"
description = "FLAME-rf utilities"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["FLAME"]
